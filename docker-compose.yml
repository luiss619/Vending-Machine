services:
  web:
    build: .
    container_name: dinowebb.vending-machine
    ports:
      - "8001:80"
    volumes:
      - .:/var/www/html
    extra_hosts:
      - "host.docker.internal:host-gateway"